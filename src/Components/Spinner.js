import spinner from './spinner.gif'

const Spinner=()=> {
  // render() {
    return (
      <div>
      <img src={spinner} alt="spinner" width={"100px"} />
      </div>
    )
  // }
}

export default Spinner;
